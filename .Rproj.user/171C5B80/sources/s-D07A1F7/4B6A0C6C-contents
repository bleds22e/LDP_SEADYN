## joining tree dynamics data

setwd("./TreeDynamics/clean_data/")
files <- list.files()

list2env(lapply(setNames(files, make.names(gsub("*.csv","", files))),
                read.csv), envir = .GlobalEnv)

dat_csv <- plyr::ldply(files, read_csv)

